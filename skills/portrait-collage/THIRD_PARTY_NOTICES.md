# Third-party notices

The bundled `assets/face_landmarker.task` is the MediaPipe Face Landmarker float16 v1 model obtained from Google's published model storage URL. Its exact source URL, documentation URL, and SHA-256 checksum are recorded in `assets/model.json`.

The Node.js and Python dependencies are third-party packages governed by their respective licenses. Exact package versions are recorded in `package-lock.json` and `requirements.txt`.

These notices are retained separately from the fork modification notice and the repository's license for its own materials.
