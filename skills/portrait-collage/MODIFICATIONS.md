# Modification notice

`portrait-collage` version 1.2.0 is an addition in the `kiedeng/gathered-scenes-zine-skill` fork. It is not an official Zeejay0 upstream skill.

It retains the source repository's paper-collage direction and attribution while adding a local landmark, similarity-alignment, adaptive transition, low-frequency face harmonization, and strict optional pixel-verification workflow. See the repository root `MODIFICATIONS.md` and `LICENSE` for provenance and permitted use.
